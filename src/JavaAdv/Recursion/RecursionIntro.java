package JavaAdv.Recursion;

/**
 * @author asus1
 * @create 2020/11/12 9:08
 *Recursion递归：
 *      直接递归：自己调用自己
 *      间接递归：A调用B，B调用C，C调用A
 *  递归需要条件限定，且递归次数不能太多，防止栈内存溢出StackOverflowError
 *  构造方法禁止递归，因为构造方法用于创建对象，递归导致内存中存在无数对象，直到编译报错。
 *
 *
 *
 */
public class RecursionIntro {
}
